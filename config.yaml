semester: 2025S

output:
   base_dir: output/

crawl:
   sleep_between_requests: 5          # seconds between requests
   resume: true                       # pick up from existing state.json if present

logging:
   level: INFO
   file: output/logs/crawler.log
